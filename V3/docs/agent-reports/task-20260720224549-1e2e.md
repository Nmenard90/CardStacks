# FND-104 — Make API container builds deterministic

## Files changed

- `apps/api/Dockerfile` — installs from the committed frozen lockfile, generates Prisma Client, builds shared/API packages, deploys an isolated production API dependency tree, and starts the compiled server directly with Node.
- `.dockerignore` — excludes secrets, host dependencies, generated output, caches, Git metadata, and documentation from Docker build contexts.
- `docs/agent-reports/task-20260720224549-1e2e.md` — records task verification and limitations.

## Acceptance criteria

- [x] `pnpm-lock.yaml` is copied before dependency installation.
- [x] Dependency installation uses `pnpm install --frozen-lockfile`.
- [x] Prisma Client generation is an explicit build step.
- [x] The runtime stage receives the API-only output of `pnpm deploy --prod`; it does not copy the full monorepo or build toolchain.
- [ ] A Docker container starts successfully (Docker is not installed in this execution environment).
- [ ] `GET /health` succeeds against the Docker container (Docker unavailable). The equivalent compiled production server startup and HTTP health request succeeded locally.
- [ ] `pnpm prepush` succeeds (the pnpm shim is absent and Corepack cannot download it because network access is blocked). API-focused compile and test checks passed; direct Turbo execution was also blocked because Turbo could not resolve the package-manager binary.
- [ ] Cross-provider review is pending Foreman commit/review orchestration.

## Checks run

- `packages/db/node_modules/.bin/prisma.cmd generate --schema packages/db/prisma/schema.prisma` — passed; Prisma Client 6.19.3 generated.
- `node_modules/.bin/tsc.cmd -p packages/shared/tsconfig.json` — passed.
- `node_modules/.bin/tsc.cmd -p apps/api/tsconfig.json` — passed.
- Compiled production startup with `NODE_ENV=production`, followed by `GET http://127.0.0.1:4317/health` — passed with `{"data":{"status":"ok"}}`.
- `apps/api/node_modules/.bin/vitest.cmd run` — passed: 3 files, 17 tests.
- `node_modules/.bin/turbo.cmd lint` — not completed; Turbo could not locate a pnpm binary. Typecheck and test portions of that chained attempt were therefore not run.
- Docker build/run — not run; Docker is unavailable.

## Known limitations and follow-up

- Run `docker build -f apps/api/Dockerfile -t tcg-v3-api .`, start the image with valid non-secret configuration, and verify `/health` in an environment with Docker.
- Run `pnpm prepush` with pnpm 9.15.0 available.
- Foreman must commit the change and obtain the required review from another provider.
