# Task task-20260722220517-e877

## Scope

Ensure Prisma Client is generated automatically in clean Foreman worktrees before database-package verification and seed commands.

## Changes

- Added one reusable `generate` command plus package lifecycle hooks for install, build, lint, typecheck, test, and seed.
- Retained the root-compatible `db:generate` alias.
- Added the regression check to the normal database test command. It invalidates a generated Prisma declaration and runs the normal database lint command to regenerate it.
- Added a `test:generate` alias so the directly documented regression command is executable.
- Updated clean-worktree setup documentation and recorded BUG-008 as fixed.

## Verification

- `node --check packages/db/scripts/verify-client-generation.mjs` — passed.
- Package JSON parsing and script assertions — passed, including the documented `test:generate` alias.
- `pnpm --filter @tcg/db lint` — attempted but not run: `pnpm` is unavailable on this sandbox PATH. Corepack could not provision the repository-declared pnpm because its cache location is outside the writable sandbox.
- `pnpm --filter @tcg/db test:generate` — not run for the same environment limitation.
- `pnpm prepush` — not run for the same environment limitation.

## Follow-up

Foreman should run the required pnpm commands in its normal verification environment, where pnpm and the installed Prisma engine are available. The independent review's fresh-install concern is not expected to block installation because `prisma generate` generates code without connecting to the configured datasource; these hooks do not run a migration or query.
