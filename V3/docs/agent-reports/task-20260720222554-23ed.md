# Task Report: task-20260720222554-23ed — FND-101 Establish reproducible clean-clone baseline

## Backlog item

`FND-101` — Establish a reproducible clean-clone baseline (`docs/BACKLOG.md`, EPIC-100), Priority P0.

Acceptance criteria (from `docs/BACKLOG.md`):

- `pnpm install --frozen-lockfile` succeeds from a clean checkout.
- No real `.env`, `node_modules`, `dist`, `.turbo`, `.metals`, `.bloop`, or `target` content is tracked.
- Setup steps in README are accurate.

## Project instruction files read

`AGENTS.md` (root + `V3/AGENTS.md`), `CLAUDE.md` (root + `V3/CLAUDE.md`), all files under `V3/.claude/rules/`, `docs/ACTIVE_TASK.md`, `docs/BACKLOG.md`, `docs/DEFINITION_OF_DONE.md`, `README.md`, `HANDOFF.md`.

## Investigation

Inspected `git ls-files` repo-wide (not only `V3/`) for `node_modules`, `.turbo`, `dist`, `build`, `target`, `.metals`, `.bloop`, `coverage`, and `.env*` patterns.

Findings:

- `.metals/metals.lock.db`, `.metals/metals.log`, `.metals/metals.mv.db` (repo root, ~4.7 MB combined) were **tracked in Git** — Scala Metals language-server generated cache/database files from the legacy `poketracker-v2` (Scala) tooling, committed long before this task. `HANDOFF.md` even referenced `git restore .metals` as a manual step in an old commit checklist, confirming this was a known rough edge, not intentional content.
- Root `.gitignore` had no entry for `.metals/`, `.bloop/`, or `target/`, so these could be re-tracked at any time.
- No `node_modules`, `dist`, `build`, `.turbo`, `.bloop`, `target`, or `coverage` content was tracked anywhere in the repo (V3 or legacy directories) — clean.
- `V3/.env` (real local secrets) is **not** tracked; only `V3/.env.example` (placeholder values) is tracked. `web/.env.production` (legacy, read-only directory) is tracked but contains only a public Vite build-time API base URL, not a secret — left untouched per the "never edit historical directories" rule.
- `V3/pnpm-lock.yaml` is tracked and up to date (`pnpm install --frozen-lockfile` reported "Lockfile is up to date, resolution step is skipped").
- No documented Node.js version existed anywhere (no `engines` field, no `.nvmrc`). `apps/api/Dockerfile` and `apps/web/Dockerfile` both pin `FROM node:22-alpine`, and `@types/node` is pinned to `^22.20.0`, establishing Node 22.x as the de facto supported/deployed version. `packageManager` already pinned `pnpm@9.15.0`.

## Changes made

1. `.gitignore` (repo root) — untracked the three `.metals/*` files (`git rm --cached`) and added a `.metals/`, `.bloop/`, `target/` ignore block so Scala/Metals build-server artifacts can't be re-committed. Did not touch anything inside `files/`, `web/`, or `poketracker-v2/`.
2. `V3/package.json` — added an `engines` field (`"node": "^22.0.0"`, `"pnpm": "^9.15.0"`) documenting the supported toolchain, matching the Dockerfiles and `@types/node`.
3. `V3/README.md` — added Node/pnpm version requirements to "Local Setup", and added a new "Verifying a Clean Clone" section documenting the exact working command order (`pnpm install --frozen-lockfile` → `pnpm db:generate` → `pnpm prepush` → `pnpm build`), including why `db:generate` must run first (generated Prisma client types are imported by `@tcg/db`, `@tcg/api`, `@tcg/worker`) and that it only needs a syntactically valid `DATABASE_URL`, not a live database.

No application source code, routes, services, repositories, or schema files were changed. No behavior change.

## Verification

Ran from `V3/` in this isolated Foreman worktree (a real clean-clone equivalent — separate checkout, separate `node_modules`):

```bash
pnpm install --frozen-lockfile
```
Result: **PASSED**. Lockfile reported up to date; install completed in ~1s. Produces one non-fatal warning: `Unsupported engine: wanted: {"node":"^22.0.0"} (current: {"node":"v24.15.0","pnpm":"9.15.0"})` — this environment's Node is 24.15.0, newer than the documented/deployed 22.x. `engine-strict` is not set, so this is a warning, not a failure (see Known Limitations).

```bash
pnpm db:generate
```
Result: **PASSED**. Regenerates the gitignored Prisma client (required before typecheck/build; not tracked source).

```bash
pnpm prepush   # lint && typecheck && test
```
Result: **PASSED**.
- `pnpm lint` — 6/6 tasks passed (`@tcg/api`, `@tcg/db`, `@tcg/shared`, `@tcg/web`, `@tcg/worker`).
- `pnpm typecheck` — 6/6 tasks passed.
- `pnpm test` — `@tcg/api`: 3 test files / 17 tests passed; `@tcg/db`, `@tcg/shared`, `@tcg/web`, `@tcg/worker` report "No test files found" and exit 0 via `--passWithNoTests` (pre-existing state; `--passWithNoTests` removal is tracked separately under `FND-103`, out of scope here).

```bash
pnpm build
```
Result: **PASSED**. 5/5 tasks; `@tcg/web` produced `dist/` assets via Vite. Confirmed `git status --short` shows no untracked `dist`/`build`/`.turbo` output afterward — ignore rules work correctly.

## Acceptance criteria status

- `pnpm install --frozen-lockfile` succeeds from the isolated checkout. ✅
- No real `.env`, `node_modules`, `dist`, `.turbo`, `.metals`, `.bloop`, or `target` content is tracked. ✅ (`.metals/*` untracked and gitignored during this task; everything else was already clean)
- Setup instructions accurately state the supported Node and pnpm versions. ✅ (`README.md` + `package.json#engines`)
- No application behavior changed. ✅ (only `.gitignore`, `package.json#engines`, `README.md`, and this report were touched; `git rm --cached` only affects Git's index, not working-tree files)
- Another provider reviews the committed changes. — Not performed by this agent; Foreman-owned per `AGENTS.md`.

## Known limitations / follow-up

- The local dev machine in this worktree runs Node `v24.15.0`, while the documented/deployed supported version is `22.x` (matching the Dockerfiles). This produces a harmless `pnpm` engine warning on every command. Recommend the team either (a) standardize local dev on Node 22.x via `nvm`/`fnm`, or (b) if Node 24 is intentionally supported going forward, widen `engines.node` and bump the Dockerfiles together — this is a product/infra decision outside FND-101's scope and is not a blocking defect.
- `--passWithNoTests` remains in `@tcg/db`, `@tcg/shared`, `@tcg/web`, `@tcg/worker` test scripts; removing it once each package has real tests is explicitly `FND-103` scope, not touched here.
- `web/.env.production` (legacy, read-only `web/` directory) is tracked and contains a non-secret public URL; left untouched per the "never edit historical directories" rule. Flagging for visibility only — no action taken or recommended within V3 scope.

## Files changed

- `.gitignore` (repo root)
- `V3/package.json`
- `V3/README.md`
- `V3/docs/agent-reports/task-20260720222554-23ed.md` (this report)
- Git index only: removed tracking of `.metals/metals.lock.db`, `.metals/metals.log`, `.metals/metals.mv.db` (files remain on disk, now gitignored)

## Next recommended backlog item

`FND-102` — Add real linting and formatting (depends on `FND-101`, now satisfied).
